import React from 'react';
import { Box, Text } from "@chakra-ui/layout";

const Logo = (props) => (
  <Box {...props}>
    <Text fontSize="lg" fontWeight="bold">
      GetFitr!
    </Text>
  </Box>
);

export default Logo;
